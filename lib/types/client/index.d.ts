/**
 * Browser client entry — the Archived Chats settings section. Renders the
 * summary strip, metadata plus conversation-content search, per-row hit
 * excerpts, and a paginated read-only conversation preview with native
 * user/right and assistant/left presentation, public Markdown/disclosure
 * primitives, a retained responsive turn rail, and authorized stored images.
 * Also renders tag/type/project filters, per-row tag chips and storage sizes,
 * single/batch backup actions, preview-first ZIP import/restore, and accessible
 * tags/note editors, all localized in English and 中文.
 */
export declare const SETTINGS_NS: string;
export declare function apply(ctx: unknown): void;
export declare const inject: string[];
