# 会话档案用户指南

[English](USER_GUIDE.md) · 简体中文 · [返回 README](../README.zh-CN.md)

本指南说明会话档案的完整用户流程、数据边界与恢复行为。Host 路由、事务和维护者实现请参阅 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 归档并查找聊天

1. 从 DSH 正常会话菜单归档聊天。
2. Host 确认成功后，全局提示会显示正在保存历史版本；只有抓取结束后，3 秒关闭计时才开始。
3. 抓取失败不会回滚已经成功的归档，提示会保留 **重试保存**、**查看**、**撤销** 和关闭操作。
4. 打开 **设置 → 会话档案**，**归档** 视图会按工作区分组显示全部可见归档聊天。
5. 使用同一个搜索框匹配标题、工作区、标签、备注、用户消息、助手回答和工具结果；正文命中会显示可读摘要。

分组折叠状态会保存在浏览器中。你可以按普通会话或子代理会话、项目和标签筛选，再按最新、最早或标题排序。

## 归档整个工作区

打开 **设置 → 会话档案**，选择 **批量归档工作区**，再从工作区选择器中选择目标。插件会准备该工作区，并打开一次包含工作区名称、符合条件会话数量和 **已归档** 去向的确认。存在正在使用的会话时，还会显示将跳过的数量，但不会列出会话身份；没有会话预览列表或第二次确认。

选择 **全部归档** 继续，或选择 **取消** 并保持所有数据不变。符合条件是指聊天当前尚未归档、没有处于非空闲状态的 Host agent，并且检查到其活动或持久化事件日志包含 `turn/start`。因此，空白的新会话窗口不属于可归档会话。旧版 Host 不提供 agent 状态时，已加载会话会被保守跳过；插件无法确认事件内容时也会跳过，而不会猜测。准备最多包含 2,000 条符合条件的聊天，并签发 5 分钟有效、只能使用一次的 token 和 nonce，绑定精确且有序的集合；执行请求不能追加 ID，准备之后新增的聊天也不会被纳入。

执行时，每条已准备会话都会重新检查工作区归属、归档状态、agent 活动状态和真实 `turn/start`。全部成功后会刷新受影响视图并关闭确认框；存在跳过、失败或历史快照失败时，Host 仍会继续处理其余项目，并保留逐项结果。快照失败会明确报告，但不会撤销已成功的归档。不确定失败后必须重新准备，并再次明确确认才能重试。此操作不会在工作区之间移动聊天，不会修改所选工作区或其目录。没有公开 `archiveSession` 的 Host 会以 `workspace-archive-unsupported` 拒绝准备请求，且不作任何修改。

## 原生只读预览

无需取消归档即可预览。预览沿用 Harness 对话布局，并支持：

- Markdown、思考过程、代码、JSON、工具调用和工具结果。
- 通过受保护本地路由读取可用的已存储图片。
- 响应式轮次导航和清楚的只读状态。
- 安全降级：附件读取不可用时，文本和工具内容仍可阅读。

关闭预览会取消尚未完成的图片和分页请求，预览不会修改来源会话。

## 标签、备注与多选

- 每条聊天最多可添加 8 个标签，每个标签最多 24 个 Unicode 字符；备注最多 2,000 个 Unicode 字符。
- 标签匹配不区分大小写；每行最多直接显示 3 个标签，其余折叠为 `+N`。
- 标签和备注只保存在本地 `metadata.json`。取消归档会保留它们，物理删除完成后才会移除。
- 每个项目聊天数量左侧保留一个全选框：勾选即选中该项目当前显示的聊天，横杠表示部分选中，点击横杠补齐全选，再次取消全选则清空该项目的选择。其他项目的选择不受影响；列表为空时隐藏选择控件。
- 有选中项时，显示每条聊天的选择框及“已选择 N 个聊天”操作栏，可导出、取消归档、移入回收站或永久删除。取消最后一条选择后自动收起，无需“完成”或“清除”按钮。
- 搜索和筛选变化会清空选择；排序和导出保留选择。批量操作只处理选中项，成功项从列表移除，失败项保留选中以便重试。永久删除确认框显示实际数量。
- 单条删除图标位于“取消归档”左侧，点击后打开该聊天的永久删除确认框。行内更多菜单提供标签备注编辑和单条导出。项目菜单提供全部取消归档、全部移至回收站和全部永久删除，范围包含该项目中被筛选隐藏的聊天。
- 右上角“全部导出”始终导出整个归档库，不受搜索、筛选和勾选影响；操作栏的“导出选中项”仅导出选中的聊天。

## 历史版本

**历史版本** 视图首次激活时才加载，并按来源聊天分组展示已验证的本地版本。

- 浏览器发起的归档成功后才保存一个已验证版本，并按相同非空来源修订去重。
- 插件不会扫描无关活动聊天，也不会在定时任务或启动时抓取。
- 每个健康版本显示时间、大小、附件数量和回收站保护状态。
- 预览只读，并明确显示快照时间。
- **恢复为副本** 会请求 Host 生成新会话 ID，并创建新的归档聊天；不会覆盖、取消归档或删除来源。
- 普通历史版本可经确认后单条删除或全局清空；只有回收站正在使用的保护版本会跳过。
- 无法读取的降级版本已不能用于恢复，但仍占用快照空间，可在同一面板中清除；若仍有回收记录引用它，则继续受保护。

## 导出、导入与恢复

可以导出单条聊天、当前选择或全部归档。每个 ZIP 包含：

```text
manifest.json
sessions/001-<安全标题>-<id>/session.json
sessions/001-<安全标题>-<id>/transcript.md
```

`session.json` 是权威机器记录，`transcript.md` 是可读副本。ZIP 路径会净化并处理重名，批量导出逐条生成。

JSON 会保留附件引用，但不包含附件二进制或后代会话。需要带完整附件的会话树时，请使用 Harness 官方 Session log 导出。

导入只接受本插件的 version 1 ZIP，并始终先预览后写入：

- 已存在的会话 ID 会标记为冲突、禁用并跳过。
- 找不到的工作区会带警告恢复为未分组归档聊天。
- 标签和备注按相同本地限制恢复。
- 导入预览不会渲染原始事件或 Markdown。
- 确认令牌 10 分钟后过期，且只能使用一次。
- 恢复优先使用 Host 的专用恢复入口；没有时改用普通的 `create` / `append` / `locate` 会话写入能力——也就是历史版本 **恢复为副本** 已经在用的那条路径。只有两者都不存在的 Host 才返回 `restore-unsupported`，且不写入任何数据。

ZIP 导入与历史版本的 **恢复为副本** 是两条独立流程。

## 回收站与永久删除

**移至回收站** 会先创建或复用健康保护快照，再提交回收记录；成功提示提供立即 **撤销**。

恢复分为两级：

1. 原会话仍完好时，只移除回收标记。
2. 原件丢失时，通过公开的 `create` / `append` 能力使用已验证快照恢复，并且绝不覆盖已有 ID。

回收站提供 **永久删除** 和 **清空回收站**。归档聊天也可从单条删除图标、项目菜单或批量操作中确认永久删除。永久删除先记录崩溃恢复意图，再删除该来源的已验证快照，**最后**才删除原会话。顺序很关键：在删除原会话之前失败的任何一步都会保留完好的聊天，下次重试仍可完成，而不会留下一条聊天已消失的回收记录。快照库中其他无法校验的快照不会阻塞永久删除——它会被跳过并上报，之后仍可从历史版本面板清除。中断的操作会在下次启动时重试。

新版 Host 只公开句柄读取、没有物理会话位置时，浏览、导出以及普通会话的保护快照仍可使用；会话目录空间显示不可用，恢复写入和永久删除会显示 Host 不支持。永久删除的拒绝发生在修改回收记录、保护快照、待处理标记或运行中会话之前。带继承历史的分支会话目前也不会被抓取为快照，因为现有快照格式无法保存继承分界；归档仍会完成，并把版本保存失败显示在结果中。

如果 `trash.json` 本身无法读取，回收站会报告不可用，并拒绝所有归档修改（取消归档、标签备注编辑、删除、永久删除），而不是靠猜测继续。归档列表仍可浏览，但会标注为未经核对——无法读取的目录无法证明哪些聊天已被删除。

删除快照附件副本不代表 Harness 全局附件存储会立即删除相同字节；其他会话引用或 Host 的垃圾回收策略仍可能保留它们。

## 空间与保留策略

空间分账会区分：

- 归档和回收会话目录。
- 插件自己的历史与保护快照。
- 无法统计或降级的项目。
- 重复快照附件字节。

可搜索的详情弹窗不会让长清单挤压主要策略视图。统计结果不会被描述为 Harness 全局可回收附件空间。

保留策略可按每个来源的历史数量、快照年龄、快照容量和回收站年龄规划，默认每个来源保留一份恢复快照。保存策略不会执行清理。每次清理都需要 5 分钟有效的单次预览、明确选择、确认和执行前重检。回收站正在使用的保护版本和不可用快照会排除，永久删除回收记录默认不选中。

## 来源与分支

只读关系视图使用 Harness 持久化 `parentSession` 展示归档或回收聊天的来源、分叉和子代理树。它只保留解释受管理会话所需的活动父子上下文，不会把无关活动聊天发送到浏览器。

搜索会展开折叠分支中的命中路径，项目和状态筛选会保留必要祖先上下文。诊断会报告缺失父节点、循环和委派深度不一致，但不会修改关系。

本版本不认识的会话头——例如更新版 Harness 新增的 origin 取值、缺失的时间戳——只会让那一个节点的细节降级，图的其余部分不受影响。节点上限针对实际展示的关系图，而不是 Harness 存储的会话总数，因此会话历史很多本身不会导致该视图不可用。

## 本地数据与隐私

插件自己的全部状态位于：

```text
$DSH_HOME/plugin-data/archived-chats/
```

目录中可能包含：

- `metadata.json`：标签和备注。
- `trash.json`：回收站记录。
- `retention.json`：已保存的保留策略。
- `snapshots/`：历史版本和保护快照。
- 尚未完成迁移时的旧版 `pending-deletions.json`。

插件不会上传、云同步或定时后台抓取会话与附件。卸载只移除插件包，并故意保留该目录，以便重新安装后继续恢复同一份状态。

## 常见问题

<details>
<summary><b>归档会删除聊天吗？</b></summary>

不会。DSH 只会把聊天从侧边栏隐藏并保留归档会话记录，会话档案提供管理入口。

</details>

<details>
<summary><b>历史版本是界面截图吗？</b></summary>

不是。它们是经过验证的本地会话记录与可用附件副本，快照预览只读。

</details>

<details>
<summary><b>恢复会覆盖来源吗？</b></summary>

不会。历史恢复会创建新的归档 ID，回收站回退恢复会拒绝已有 ID，两条路径都不会覆盖来源。

</details>

<details>
<summary><b>导入 ZIP 包含已有 ID 时会怎样？</b></summary>

该行会标记为冲突、禁用并跳过。导入绝不会覆盖已有会话。

</details>

<details>
<summary><b>为什么归档列表为空时仍有快照？</b></summary>

恢复回收聊天会移除回收记录，但保留已验证快照作为历史。确认不再需要时，可明确删除普通历史，或应用经过预览的保留策略。

</details>

<details>
<summary><b>降级或删除插件数据前应该做什么？</b></summary>

先恢复仍需使用的内容，并备份完整插件数据目录。旧版本可能不显示历史版本或不识别较新的回收快照。

</details>
